import AbstractSpell from "../../AbstractSpell";
import RunItBackImage from "../../../../../assets/img/agents/phoenix/Run_it_Back.webp";

export default class RunItBack extends AbstractSpell {
  constructor() {
    super("Phoenix", "Run It Back", RunItBackImage, "ultimate");
  }
}
